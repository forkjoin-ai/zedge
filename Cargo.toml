[package]
name = "zedge"
version = "0.1.0"
edition = "2021"
description = "Edge inference coding IDE — AI that runs where you are"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
zed_extension_api = "0.7"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
gnosis-betti-wasm = { path = "../gnosis/gnosis-betti-wasm" }

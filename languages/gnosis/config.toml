name = "Gnosis"
type = "language"
path_suffixes = ["gg", "ggl", "gnarly"]
line_comments = ["//"]
# Use Cypher as a fallback for highlighting if available
grammar = "gnosis"

[language_server.gnosis-lsp]
# This links the language to the LSP defined in extension.toml

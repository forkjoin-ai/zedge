# Gnosis Grammar

Parent: [Zedge Grammars](../README.md)

The Gnosis tree-sitter grammar covers `.gg`, `.ggl`, and `.gnarly` topology
files. Gnarly adds `gnarly { ... }` metadata and nested `impl <node> in
<language> { ... }` implementation blocks on top of GG node and edge syntax.
